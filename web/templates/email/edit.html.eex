<h2>Edit email</h2>

<%= render "form.html", changeset: @changeset,
                        action: email_path(@conn, :update, @email),
                        name_brands: @name_brands %>

<%= link "Back", to: email_path(@conn, :index) %>
